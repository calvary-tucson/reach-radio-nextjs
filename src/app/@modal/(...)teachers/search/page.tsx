// TeacherSearchSheetContent (rendered directly by ModalLayout when
// rootSheet === 'search') owns the sheet's visible content for soft
// navigation. This route only exists so /teachers/search participates in
// Next's URL/history bookkeeping for the intercepted-route slot.
export default function TeachersSearchSheetPage() {
  return null
}
